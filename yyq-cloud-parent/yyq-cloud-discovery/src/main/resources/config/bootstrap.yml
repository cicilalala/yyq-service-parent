server:
  port: 8761
spring:
  application:
    name: yyq-cloud-discovery
  profiles:
    active: local
eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false