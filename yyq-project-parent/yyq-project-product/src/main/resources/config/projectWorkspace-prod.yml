spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clusterDB?characterEncoding=UTF-8&userSSL=false&serverTimezone=GMT%2b8
    username: root
    password: admin123
    driverClassName: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

# 返回结果不含null
  jackson:
    default-property-inclusion: non_null
  freemarker:
    cache: false
server:
  context-path: /sell/